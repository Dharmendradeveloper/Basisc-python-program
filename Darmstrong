num1=int(input("enter the num1"))
num2=int(input("enter the num2"))
for num in range(num1,num2 +1):
    order=len(str(num))
    sum=0
    temp=num
    while temp>0:
        digits=temp%10
        sum+=digits**order
        temp//=10
    if num==sum:
        print(sum)
        
