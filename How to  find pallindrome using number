num=int(input("enter a num"))
order=len(str(num))
sum=0
temp=num
while temp>0:
    digits=temp%10
    sum+=digits**order
    temp=temp//10
if(sum==num):
    print("pallindrome")
else:
    print("not pallindrome")
